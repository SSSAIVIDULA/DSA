REMOVE OUTER PARENTHESES
class Solution {
    public String removeOuterParentheses(String s) {
        String str="";
        int count=0;
        for(int i=0;i<s.length();i++){
            if(s.charAt(i)=='('){
                count++;
              if(count>1 ){
                str+=s.charAt(i);
              }
            }
              
            
            
            if(s.charAt(i)==')'){
                count--;
              if(count>=1 ){
                str+=s.charAt(i);
            }
            }
             
            
        }
        return str;
    }
}
---------------------------------------------------------------------------------------
LONGEST ODD NUMBER IN A STRING 
import java.util.*;
class sol{
  public static void main(String[] args){
    Scanner sc=new Scanner(System.in);
    String str=sc.next();
    int index=0;
    for(int i=str.length()-1;i>=0;i--){
      char ch=str.charAt(i);
      if((ch-'0') %2==1){
        index=i;
        break;
    }
  }
  int i=0;
  while(str.charAt(i)=='0'){
    i++;
  }
  System.out.print(str.substring(i,index+1));
  
  }
}
--------------------------------------------------------------------------------------
LONGEST COMMON PREFIX
import java.util.*;
class sol{
    public static void main(String[] args){
        Scanner sc=new Scanner(System.in);
        int n=sc.nextInt();
        String[] words=new String[n];
        for(int i=0;i<n;i++){
            words[i]=sc.next();
        }
        String compare=words[0];
        String ans="";
        String check="";
        for(int i=1;i<n;i++){
             check=words[i];
            int min=Math.min(check.length(),compare.length());
            int k=0;
            for(int j=0;j<min;j++){
                if(check.charAt(j)==compare.charAt(j)){
                    ans+=check.charAt(j);
                }
                else{
                    break;
                }
            }
            compare=ans.toString();
            ans="";
        }
        System.out.print(compare);
    }
}

SORT & COMPARE FIRST AND LAST
import java.util.*;
class sol{
    public static void main(String[] args){
        Scanner sc=new Scanner(System.in);
        int n=sc.nextInt();
        String[] words=new String[n];
        for(int i=0;i<n;i++){
            words[i]=sc.next();
        }
        StringBuilder ans=new StringBuilder();
        Arrays.sort(words);
        String first=words[0];
        String last=words[n-1];
        for(int i=0;i<first.length();i++){
            if(first.charAt(i)==last.charAt(i)){
                ans.append(first.charAt(i));
            }
            else{
                break;
            }
        }
        System.out.print(ans);
        
    }
}
-------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------
